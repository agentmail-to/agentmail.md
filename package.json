{
  "name": "agentmail-md",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "build:local": "npm run build && node scripts/build-local.js",
    "build:hermes": "node scripts/build-hermes.js"
  }
}
