# Core

The core AgentMail reference now lives in [cli.md](cli.md). Use it for inboxes,
messages, threads, replies, forwarding, labels, and attachments.

Use [fallbacks.md](fallbacks.md) only when the CLI is unavailable or a REST/MCP
integration is explicitly required.
